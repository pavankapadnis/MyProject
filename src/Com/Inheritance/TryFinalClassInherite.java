package Com.Inheritance;

public class TryFinalClassInherite extends FinalInheritanceClass {

	/*
	 * Here we try the inherite FinalInheritanceClass but there is an error because
	 * th final class doesnt particape in inheritance.
	 * 
	 * Also we cannot override Final Method.
	 * 
	 * Also we Cannot Override Static Method.
	 */
}
